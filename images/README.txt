Put your photo files in this folder (jpg, png, or webp all work).

Then open photos.html and add a line to the `photos` array near the
top of the <script> tag for each image, for example:

  { src: "images/graduation.jpg", caption: "UCSC graduation, June 2026" }

The caption is optional.
